<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/navigation_episodes"
    android:id="@+id/nav_graph">

    <fragment
        android:id="@+id/navigation_episodes"
        android:name="ankit.com.starwarepisodes.view.episodes.EpisodesFragment"
        android:label="@string/episodes"
        tools:layout="@layout/episodes_fragment" />

</navigation>